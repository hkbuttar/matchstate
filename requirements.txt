# Exact pins, not ranges: this stack's dependency tree (pymc -> pytensor ->
# numba -> llvmlite, in particular) is large and interlinked enough that
# loose ">=" bounds send pip's resolver into a combinatorial search it
# can't finish -- hit this directly as a real Render build failure
# (pip._vendor.resolvelib.resolvers.ResolutionTooDeep) before pinning.
# Versions below are frozen from a verified-working local install; bump
# deliberately (and re-verify: `pytest tests/ -v`, backend/frontend smoke
# test) rather than loosening back to ranges.

# Core data science
numpy==1.26.4
pandas==3.0.5
scipy==1.17.1

# Modeling
scikit-learn==1.9.0
xgboost==3.2.0
pymc==5.25.1
arviz==0.23.4

# Backend / API
fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4

# Columnar storage (event-level data)
pyarrow==25.0.0

# Data acquisition
requests==2.34.2
httpx==0.28.1
beautifulsoup4==4.15.0
lxml==6.1.1

# StatsBomb open data
statsbombpy==1.22.0

# Notebooks
jupyter==1.1.1
ipykernel==7.3.0

# Viz (research/EDA use; the dashboard itself is the Next.js frontend)
matplotlib==3.11.1
seaborn==0.13.2

# Testing
pytest==9.1.1
pytest-cov==7.1.0
